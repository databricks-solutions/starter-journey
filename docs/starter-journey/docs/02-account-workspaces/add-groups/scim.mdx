---
sidebar_label: SCIM
description: Automate group provisioning into Databricks using SCIM from your identity provider.
---

import { StepGuide, Step } from '@site/src/components/StepGuide';

# Add Groups: SCIM

> **You'll configure** SCIM to automatically sync groups from your identity provider into Databricks in ~10 min.
>
> **Prereqs:** [Add Users: SCIM](/docs/02-account-workspaces/add-users/scim) (SCIM connector already configured)

## The big picture

Your identity provider's groups synced into the Databricks account. When membership changes in the IdP, it changes in Databricks too, with no manual step in between.

## Prerequisites

- SCIM connector already configured (see [Add Users: SCIM](/docs/02-account-workspaces/add-users/scim)).
- Admin access to your identity provider.

## Steps

<StepGuide
  description="Groups ride on the same SCIM connector you set up for users. Configure sync in your IdP, assign the groups, and trigger a sync."
  minutes={10}
  referenceHref="https://docs.databricks.com/aws/en/admin/users-groups/scim/"
  referenceLabel="SCIM provisioning overview (Databricks docs)">

<Step>

Configure group sync in your identity provider. If you already set up SCIM for users, groups ride on the same connector, so follow the guide for your IdP:

| Identity provider | Guide |
|---|---|
| Microsoft Entra ID (Azure AD) | [Configure SCIM provisioning for Entra ID](https://docs.databricks.com/aws/en/admin/users-groups/scim/aad) |
| Okta | [Configure SCIM provisioning for Okta](https://docs.databricks.com/aws/en/admin/users-groups/scim/okta) |
| OneLogin | [Configure SCIM provisioning for OneLogin](https://docs.databricks.com/aws/en/admin/users-groups/scim/onelogin) |

If the steps differ by cloud, select your cloud provider at the top right of the Databricks docs page.

</Step>

<Step>

In your identity provider, assign the groups you want to the Databricks SCIM application. Only assigned groups sync, so anything you leave out simply will not show up.

</Step>

<Step>

Run a manual sync from your IdP, or wait for the next scheduled cycle. The groups should appear in the Databricks account console.

</Step>

</StepGuide>

## Verify

<StepGuide eyebrow="Verify" idPrefix="verify">

<Step>

Go to **User management** > **Groups** in the account console.

</Step>

<Step>

Confirm the synced groups have the right names and members.

</Step>

<Step>

In your IdP, check that the SCIM application reports a successful provisioning status for groups.

</Step>

</StepGuide>

## Where people trip

<details>
<summary>Groups not appearing after sync</summary>

Check that the groups are assigned to the SCIM application in your IdP. Unassigned groups never get pushed. If they are assigned and still missing, read the SCIM application provisioning logs for errors.
</details>

<details>
<summary>Group members missing after sync</summary>

SCIM syncs membership from the IdP side. If someone is in the group in your IdP but not in Databricks, check that the user is also assigned to the SCIM application on their own, not just through the group.
</details>

## Next

- **Do next:** [Set admin group](/docs/02-account-workspaces/metastore-admins/set-admin-group)
- **Learn why:** [Unity Catalog foundations](/docs/01-introduction/foundations/unity-catalog)
- **Reference:** [SCIM provisioning overview](https://docs.databricks.com/aws/en/admin/users-groups/scim/)
